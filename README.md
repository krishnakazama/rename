# Rename directories (spaces → underscores)

A safe Bash script using GNU parallel to rename directory names
by replacing spaces with underscores.

## Requirements
- GNU parallel

## Usage

Dry run:
```bash
./rename_dirs_spaces_to_underscore.sh --dry-run

## Execute
/rename_dirs_spaces_to_underscore.sh
